import Index from '@/views/admin/artists/Index.vue'

const routes = [
    {
      path: '/admin/artists',
      name: 'admin.artists.index',
      component: Index,
    }
  ]
  
  export default routes
